description: missing hard binding assertion in standard manifest (claim.hardBindings.missing)
inputs:
  assetPath: assets/hard_bindings_missing.jpg
  claimSignerTrustListPaths:
  - certs/root_ca1_cert.pem
  tsaTrustListPaths:
  - certs/root_ca1_cert.pem
  validationTime: "2001-06-01T00:00:00Z"
manifests:
- failures:
    containsAllOf:
      codes:
      - claim.hardBindings.missing
