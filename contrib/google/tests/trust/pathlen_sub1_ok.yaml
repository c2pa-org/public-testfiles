description: intermediate CA pathlen constraint met (Root -> Sub1 pathlen:1 -> Sub2
  -> Signer)
inputs:
  assetPath: assets/pathlen_sub1_ok.jpg
  claimSignerTrustListPaths:
  - certs/root_ca1_cert.pem
  tsaTrustListPaths:
  - certs/root_ca1_cert.pem
  validationTime: "2001-06-01T00:00:00Z"
manifests:
- failures:
    isEmpty: {}
  successes:
    containsAllOf:
      codes:
      - claimSignature.validated
      - signingCredential.trusted
