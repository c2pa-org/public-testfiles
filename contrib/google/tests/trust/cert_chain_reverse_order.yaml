description: COSE x5chain in reverse order (leaf-last)
inputs:
  assetPath: assets/issuing_ca_cert_included.jpg
  claimSignerTrustListPaths:
  - certs/root_ca1_cert.pem
  tsaTrustListPaths:
  - certs/root_ca1_cert.pem
  validationTime: "2001-06-01T00:00:00Z"
manifests:
- failures:
    isEmpty: {}
  successes:
    containsAllOf:
      codes:
      - claimSignature.validated
      - signingCredential.trusted
